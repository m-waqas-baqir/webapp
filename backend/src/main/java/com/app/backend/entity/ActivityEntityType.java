package com.app.backend.entity;

/** Domain aggregate type for activity logging (subset of modules). */
public enum ActivityEntityType {
    PLOT,
    RENTAL_PROPERTY,
    OWNER,
    PHASE,
    KHAYABAN,
    LISTING,
    USER
}
